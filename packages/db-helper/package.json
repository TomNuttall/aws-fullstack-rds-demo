{
  "name": "db-helpers",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "yarn drizzle-kit generate",
    "generate:new": "yarn generate --custom",
    "migrate:local": "yarn tsx -r dotenv/config utils/migrate.ts utils/drizzle.config.ts",
    "studio": "yarn drizzle-kit studio",
  },
  "dependencies": {
    "drizzle-orm": "^0.32.0",
    "mysql2": "^3.10.3"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^20.1.3",
    "@types/node": "^20.14.11",
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.23.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}

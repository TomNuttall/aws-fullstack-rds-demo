{
  "name": "demo_rds",
  "version": "1.0.0",
  "author": "Tom Nuttall <tsnuttall@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev:api": "cd lambdas/api && yarn dev",
    "dev:app": "cd apps/client && yarn dev",
    "db:start": "cd docker && docker compose up -d",
    "db:migrate": "cd lambda/migrations && yarn migrate",
    "db:migrate:test": "cd lambda/migrations && yarn migrate:test",
    "db:stop": "cd docker && docker compose down"
  }
}
